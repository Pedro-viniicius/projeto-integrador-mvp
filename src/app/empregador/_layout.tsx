import React from 'react';
import { AppShell } from '@/components/navigation/AppShell';

/** Área do empregador: Início · Minhas vagas · Candidatos · Perfil. */
export default function EmployerLayout() {
  return <AppShell />;
}
